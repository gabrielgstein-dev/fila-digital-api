name: Deploy Backend to Cloud Run (Staging)

on:
  push:
    tags:
      - '*-stage'
  workflow_dispatch:
    inputs:
      tag:
        description: 'Tag to deploy'
        required: true
        default: 'latest-stage'

env:
  GCP_PROJECT_ID: ${{ secrets.GCP_PROJECT_ID_STAGE }}
  GCP_REGION: ${{ secrets.GCP_REGION_STAGE }}
  SERVICE_NAME: ${{ secrets.BACKEND_SERVICE_NAME_STAGE }}

jobs:
  deploy:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Extract version from tag
      id: version_check
      run: |
        if [[ "${{ github.event_name }}" == "push" && "${{ github.ref_type }}" == "tag" ]]; then
          # Extract version from tag
          TAG_NAME=${GITHUB_REF#refs/tags/}
          echo "Tag detectada: $TAG_NAME"
          
          if [[ "$TAG_NAME" == *"-stage" ]]; then
            echo "should_deploy=true" >> $GITHUB_OUTPUT
            echo "version=$TAG_NAME" >> $GITHUB_OUTPUT
            echo "✅ Deploy será executado para versão: $TAG_NAME"
          else
            echo "should_deploy=false" >> $GITHUB_OUTPUT
            echo "❌ Tag não contém '-stage', deploy cancelado"
          fi
        elif [[ "${{ github.event_name }}" == "workflow_dispatch" ]]; then
          # Manual dispatch
          TAG_NAME="${{ github.event.inputs.tag }}"
          echo "should_deploy=true" >> $GITHUB_OUTPUT
          echo "version=$TAG_NAME" >> $GITHUB_OUTPUT
          echo "✅ Deploy manual para versão: $TAG_NAME"
        else
          echo "should_deploy=false" >> $GITHUB_OUTPUT
          echo "❌ Evento não suportado para deploy"
        fi

    - name: Authenticate to Google Cloud
      if: steps.version_check.outputs.should_deploy == 'true'
      uses: google-github-actions/auth@v2
      with:
        credentials_json: ${{ secrets.GCP_SA_KEY_STAGE }}

    - name: Setup Google Cloud CLI
      if: steps.version_check.outputs.should_deploy == 'true'
      uses: google-github-actions/setup-gcloud@v2
      with:
        project_id: ${{ secrets.GCP_PROJECT_ID_STAGE }}

    - name: Configure Docker to use gcloud as a credential helper
      if: steps.version_check.outputs.should_deploy == 'true'
      run: |
        echo "Configurando autenticação..."
        gcloud auth list
        gcloud config list
        gcloud auth configure-docker gcr.io --quiet



    - name: Build Docker image
      if: steps.version_check.outputs.should_deploy == 'true'
      run: |
        VERSION="${{ steps.version_check.outputs.version }}"
        echo "🔍 Debug Info:"
        echo "GCP_PROJECT_ID: $GCP_PROJECT_ID"
        echo "SERVICE_NAME: $SERVICE_NAME"
        echo "VERSION: $VERSION"
        
        if [[ -z "$GCP_PROJECT_ID" ]]; then
          echo "❌ GCP_PROJECT_ID está vazio"
          exit 1
        fi
        
        if [[ -z "$SERVICE_NAME" ]]; then
          echo "❌ SERVICE_NAME está vazio"
          exit 1
        fi
        
        docker build --platform linux/amd64 -f Dockerfile.qa -t gcr.io/$GCP_PROJECT_ID/$SERVICE_NAME:$VERSION \
                     -t gcr.io/$GCP_PROJECT_ID/$SERVICE_NAME:$GITHUB_SHA \
                     -t gcr.io/$GCP_PROJECT_ID/$SERVICE_NAME:latest .

    - name: Push Docker image
      if: steps.version_check.outputs.should_deploy == 'true'
      run: |
        VERSION="${{ steps.version_check.outputs.version }}"
        echo "🔍 Debug antes do push:"
        echo "Imagens construídas:"
        docker images | grep gcr.io/$GCP_PROJECT_ID/$SERVICE_NAME
        echo "Verificando autenticação:"
        gcloud auth list
        echo "Fazendo push das imagens:"
        docker push gcr.io/$GCP_PROJECT_ID/$SERVICE_NAME:$VERSION
        docker push gcr.io/$GCP_PROJECT_ID/$SERVICE_NAME:$GITHUB_SHA
        docker push gcr.io/$GCP_PROJECT_ID/$SERVICE_NAME:latest

    - name: Deploy to Cloud Run
      if: steps.version_check.outputs.should_deploy == 'true'
      run: |
        gcloud run deploy "$SERVICE_NAME" \
          --image "gcr.io/$GCP_PROJECT_ID/$SERVICE_NAME:$GITHUB_SHA" \
          --region "$GCP_REGION" \
          --platform managed \
          --allow-unauthenticated \
          --port 8080 \
          --memory 2Gi \
          --cpu 2 \
          --min-instances 0 \
          --max-instances 10 \
          --timeout 3600 \
          --execution-environment gen2 \
          --cpu-boost \
          --set-env-vars "NODE_ENV=${{ secrets.NODE_ENV_STAGE }}" \
          --set-env-vars "DATABASE_URL=${{ secrets.DATABASE_URL_STAGE }}" \
          --set-env-vars "JWT_SECRET=${{ secrets.JWT_SECRET_STAGE }}" \
          --set-env-vars "RABBITMQ_URL=${{ secrets.RABBITMQ_URL_STAGE }}"

    - name: Get service URL
      if: steps.version_check.outputs.should_deploy == 'true'
      run: |
        SERVICE_URL=$(gcloud run services describe $SERVICE_NAME --region $GCP_REGION --format="value(status.url)")
        VERSION="${{ steps.version_check.outputs.version }}"
        echo "🚀 Backend deployed successfully!"
        echo "📦 Version: $VERSION"
        echo "🌐 URL: $SERVICE_URL"
        echo "📚 API Docs: $SERVICE_URL/api"

    - name: Skip deployment message
      if: steps.version_check.outputs.should_deploy == 'false'
      run: |
        echo "⏭️ Deploy pulado - Tag não é válida para staging"
        echo "💡 Para fazer deploy, execute um dos comandos:"
        echo "   pnpm run version:patch  (cria tag X.X.X-stage)"
        echo "   pnpm run version:minor  (cria tag X.X.X-stage)"
        echo "   pnpm run version:major  (cria tag X.X.X-stage)"
        echo ""
        echo "🏷️ Tags válidas devem terminar com '-stage'"
