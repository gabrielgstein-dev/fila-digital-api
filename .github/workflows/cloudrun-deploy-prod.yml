name: Deploy Backend to Cloud Run (Production)

on:
  push:
    tags:
      - '[0-9]+.[0-9]+.[0-9]+'  # Apenas tags sem sufixo (ex: 1.2.3)
  workflow_dispatch:
    inputs:
      tag:
        description: 'Production tag to deploy (without -stage suffix)'
        required: true
        default: '1.0.0'

env:
  GCP_PROJECT_ID: ${{ secrets.GCP_PROJECT_ID_PROD }}
  GCP_REGION: ${{ vars.GCP_REGION_PROD }}
  SERVICE_NAME: ${{ vars.BACKEND_SERVICE_NAME_PROD }}

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment: production  # Requer aprovação/permissão

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Extract version from tag
      id: version_check
      run: |
        if [[ "${{ github.event_name }}" == "push" && "${{ github.ref_type }}" == "tag" ]]; then
          # Extract version from tag
          TAG_NAME=${GITHUB_REF#refs/tags/}
          echo "Tag detectada: $TAG_NAME"

          # Validar que NÃO contém -stage e segue formato X.Y.Z
          if [[ "$TAG_NAME" =~ ^[0-9]+\.[0-9]+\.[0-9]+$ ]]; then
            echo "should_deploy=true" >> $GITHUB_OUTPUT
            echo "version=$TAG_NAME" >> $GITHUB_OUTPUT
            echo "✅ Deploy de PRODUÇÃO será executado para versão: $TAG_NAME"
          else
            echo "should_deploy=false" >> $GITHUB_OUTPUT
            echo "❌ Tag não é válida para produção (deve ser X.Y.Z sem sufixo)"
          fi
        elif [[ "${{ github.event_name }}" == "workflow_dispatch" ]]; then
          # Manual dispatch
          TAG_NAME="${{ github.event.inputs.tag }}"
          echo "should_deploy=true" >> $GITHUB_OUTPUT
          echo "version=$TAG_NAME" >> $GITHUB_OUTPUT
          echo "✅ Deploy manual de PRODUÇÃO para versão: $TAG_NAME"
        else
          echo "should_deploy=false" >> $GITHUB_OUTPUT
          echo "❌ Evento não suportado para deploy de produção"
        fi

    - name: Setup Google Cloud CLI
      if: steps.version_check.outputs.should_deploy == 'true'
      uses: google-github-actions/setup-gcloud@v2
      with:
        service_account_key: ${{ secrets.GCP_SA_KEY_PROD }}
        project_id: ${{ secrets.GCP_PROJECT_ID_PROD }}

    - name: Configure Docker to use gcloud as a credential helper
      if: steps.version_check.outputs.should_deploy == 'true'
      run: gcloud auth configure-docker

    - name: Build Docker image
      if: steps.version_check.outputs.should_deploy == 'true'
      run: |
        VERSION="${{ steps.version_check.outputs.version }}"
        docker build -t gcr.io/$GCP_PROJECT_ID/$SERVICE_NAME:$VERSION \
                     -t gcr.io/$GCP_PROJECT_ID/$SERVICE_NAME:$GITHUB_SHA \
                     -t gcr.io/$GCP_PROJECT_ID/$SERVICE_NAME:latest .

    - name: Push Docker image
      if: steps.version_check.outputs.should_deploy == 'true'
      run: |
        VERSION="${{ steps.version_check.outputs.version }}"
        docker push gcr.io/$GCP_PROJECT_ID/$SERVICE_NAME:$VERSION
        docker push gcr.io/$GCP_PROJECT_ID/$SERVICE_NAME:$GITHUB_SHA
        docker push gcr.io/$GCP_PROJECT_ID/$SERVICE_NAME:latest

    - name: Deploy to Cloud Run
      if: steps.version_check.outputs.should_deploy == 'true'
      run: |
        echo "🚀 Iniciando deploy para produção..."
        gcloud run deploy $SERVICE_NAME \
          --image gcr.io/$GCP_PROJECT_ID/$SERVICE_NAME:$GITHUB_SHA \
          --region $GCP_REGION \
          --platform managed \
          --allow-unauthenticated \
          --port 8080 \
          --memory 2Gi \
          --cpu 2 \
          --min-instances 1 \
          --max-instances 50 \
          --set-env-vars NODE_ENV=production \
          --set-env-vars ENVIRONMENT=production \
          --set-env-vars CORS_ORIGIN=https://fila-digital.com,https://www.fila-digital.com,https://app.fila-digital.com \
          --set-env-vars DATABASE_PROVIDER=render \
          --set-env-vars DATABASE_URL=${{ secrets.DATABASE_URL_PROD }} \
          --set-env-vars JWT_SECRET=${{ secrets.JWT_SECRET_PROD }} \
          --set-env-vars JWT_EXPIRES_IN=7d \
          --set-env-vars RABBITMQ_URL=${{ secrets.RABBITMQ_URL_PROD }} \
          --set-env-vars WHATSAPP_MIN_DELAY_MS=${{ secrets.WHATSAPP_MIN_DELAY_MS_PROD }} \
          --set-env-vars META_API_VERSION=${{ secrets.META_API_VERSION_PROD }} \
          --set-env-vars META_PHONE_NUMBER_ID=${{ secrets.META_PHONE_NUMBER_ID_PROD }} \
          --set-env-vars META_ACCESS_TOKEN=${{ secrets.META_ACCESS_TOKEN_PROD }} \
          --set-env-vars FRONTEND_URL=${{ secrets.FRONTEND_URL_PROD }}

    - name: Run Prisma Migrations (Production)
      if: steps.version_check.outputs.should_deploy == 'true'
      run: |
        echo "🔒 Iniciando migrações do banco de dados em produção..."

        # Instalar o Prisma CLI globalmente
        echo "📦 Instalando Prisma CLI..."
        npm install -g prisma

        # Configurar variáveis de ambiente para o Prisma
        export DATABASE_URL="${{ secrets.DATABASE_URL_PROD }}"
        export JWT_SECRET="${{ secrets.JWT_SECRET_PROD }}"

        # Verificar migrações pendentes (apenas para log)
        echo "🔍 Verificando migrações pendentes..."
        npx prisma migrate status || echo "⚠️ Não foi possível verificar o status das migrações (isso é normal na primeira execução)"

        # Aplicar migrações (modo seguro para produção)
        echo "🚀 Aplicando migrações em produção..."
        npx prisma migrate deploy --schema=./prisma/schema.prisma

        # Verificar o status após a migração
        echo "✅ Migrações aplicadas com sucesso em produção!"
        npx prisma migrate status || true
      env:
        DATABASE_URL: ${{ secrets.DATABASE_URL_PROD }}
        JWT_SECRET: ${{ secrets.JWT_SECRET_PROD }}

    - name: Get service URL
      if: steps.version_check.outputs.should_deploy == 'true'
      run: |
        SERVICE_URL=$(gcloud run services describe $SERVICE_NAME --region $GCP_REGION --format="value(status.url)")
        VERSION="${{ steps.version_check.outputs.version }}"
        echo "🚀 Backend PRODUÇÃO deployed successfully!"
        echo "📦 Version: $VERSION"
        echo "🌐 URL: $SERVICE_URL"
        echo "📚 API Docs: $SERVICE_URL/api"

    - name: Skip deployment message
      if: steps.version_check.outputs.should_deploy == 'false'
      run: |
        echo "⏭️ Deploy pulado - Tag não é válida para produção"
        echo "💡 Para deploy de produção, use:"
        echo "   pnpm run version:prod-patch  (cria tag X.X.X)"
        echo "   pnpm run version:prod-minor  (cria tag X.X.X)"
        echo "   pnpm run version:prod-major  (cria tag X.X.X)"
        echo ""
        echo "🏷️ Tags de produção devem seguir formato X.Y.Z (sem sufixo)"
        echo "🔒 Ambiente de produção requer permissões especiais"
